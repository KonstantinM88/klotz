NEXT_PUBLIC_SITE_URL=http://localhost:3000
DEMO_MODE=true
# Optional: enables live AI answers in the project guide. Keep the key in .env.local only.
# OPENAI_API_KEY=
OPENAI_MODEL="gpt-5.4-mini"
# Indexing stays disabled in code until a separately approved production release.
# DEMO_MODE=false disables the validation endpoint; it does not enable delivery.
