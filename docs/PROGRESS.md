# Progress

Последнее обновление: 2026-09-26.

## Текущая стадия

- [x] Изучен Lastenheft заказчика.
- [x] Проверены ключевые страницы и публичные материалы текущего KLOTZ.mobi.
- [x] Зафиксирована граница: KLOTZ demo, без CMS/CRM и обязательной БД.
- [x] Проверен стабильный baseline Next.js/React/Prisma через npm registry и strict peer resolution.
- [x] Подготовлены master prompt, AGENTS и supporting documentation.
- [x] Подготовлена визуальная концепция и содержательный wireframe главной страницы.
- [x] Выполнен обзор premium-производителей и региональной конкурентной выдачи.
- [x] Созданы desktop- и mobile-preview главной через image generation; это концепты, не утверждённые project assets.
- [x] Получена явная команда на реализацию.
- [ ] Выполнен полный crawl legacy-сайта и media inventory.
- [x] Создан Next.js-проект.
- [x] Реализована главная, ключевые разделы, референсы, Wissen и demo-форма.
- [x] Выполнены typecheck, lint, unit tests, production build, route smoke test и browser QA.
- [ ] Проведены ручные screen-reader и real-device checks после утверждения контента.
- [ ] Демо опубликовано.

Первичная фиксация и push ранее выполнены по отдельной команде пользователя:
`f748470 chore: initialize KLOTZ demo`, remote `https://github.com/KonstantinM88/klotz.git`.
Новые презентационные изменения пока локальные: без commit/push/deployment.

## Важная граница

Проект и зависимости созданы локально. Демо не задеплоено и не опубликовано; индексирование по умолчанию запрещено.

### 2026-09-26 — обход разрешён, индексация запрещена

По просьбе пользователя `robots.txt` теперь разрешает обход (`Allow: /`). Директива `noindex` остаётся в HTML и HTTP-заголовке, `nofollow` снят для переходов по ссылкам; sitemap по-прежнему пуст. Такое сочетание позволяет ботам прочесть запрет индексации. Внешний preview требует защиты паролем, если контент должен быть закрыт от посторонних.

Проверено локально: Prettier, TypeScript, ESLint, `git diff --check`, 21/21 unit/component tests и production build (53 записи). Route check: 45 HTML-страниц, 1 redirect и 45 внутренних ссылок. Playwright: все 32 теста на desktop/mobile показали `ok`, включая `robots.txt`, `noindex`, canonical, JSON-LD, клавиатуру и axe; после последнего теста runner не завершился самостоятельно и был остановлен. Скриншоты главной, услуги, референса, статьи, формы и «Über uns» на mobile визуально просмотрены.

Lighthouse mobile (главная / услуга / референс / статья / форма / «Über uns»): Performance **80 / 85 / 87 / 96 / 86 / 80**, Accessibility и Best Practices **100**, SEO **69** из-за намеренного `noindex`; CLS **0**. Цель Performance ≥95 остаётся открытой. Временный сервер на порту 3100 остановлен; commit/push/deployment, публикация и production-проверка не выполнялись.

### 2026-09-25 — команда на странице «Über uns»

По новой явной просьбе пользователя перенесены все восемь карточек старого сайта: Herr Klotz, Frau Shafiee, Herr Walther, Herr Kimmel, Montageteam, Frau Kriester, Herr Temgoua и Herr Degenhardt. Имена и должности сохранены как в источнике; индивидуальные пояснения описывают только заявленные роли. Семь портретов показаны карточками, общий снимок монтажной команды — отдельным широким блоком. На странице есть пометка `Demo / noch abzustimmen` о необходимости проверить актуальность состава, функций и учебных годов. Данные находятся в типизированном repository, без Person JSON-LD.

Все восемь фото загружены с legacy-страницы. Исходники, оптимизированные локальные WebP и индивидуальные URL/SHA-256/права для презентации зафиксированы в `content/assets-manifest.json`; импорт воспроизводится скриптом `scripts/import-company-team.mjs`. Новых зависимостей нет. Фото и имена предназначены только для этой клиентской презентации, не для публичной публикации.

Проверено локально: форматирование, TypeScript, ESLint, 21/21 unit/component tests, production build (53 записи), Playwright **32/32** на desktop/mobile. Проверены все восемь карточек, загрузка изображений, клавиатура, axe, canonical, noindex, JSON-LD и отсутствие горизонтального переполнения. Route check: 45 HTML-страниц, 1 redirect, 45 внутренних ссылок. Визуально просмотрена «Über uns» на desktop/mobile.

Lighthouse mobile (главная / услуга / референс / статья / форма / «Über uns»): Performance **81 / 87 / 89 / 89 / 85 / 90**, Accessibility и Best Practices **100**, SEO **69** из-за намеренного noindex. На «Über uns» LCP 2,99 с и CLS 0; целевые Performance ≥95 пока не достигнуты. Разница с предыдущим замером 81 для этой страницы не доказывает ускорение: локальные прогоны колеблются.

Для бизнеса: **лучше** — реальные лица и роли делают компанию понятнее и убедительнее. **Рискованнее** — кадровые сведения и права на публичную публикацию портретов могут измениться; перед production нужно подтверждение KLOTZ. Commit/push/deployment и публикация не выполнялись, production не проверялся.

### 2026-09-25 — полноценная страница «Über uns»

По запросу пользователя черновой раздел `/unternehmen` превращён в содержательную страницу «Über uns». Пункт с таким названием доступен в desktop header, мобильном меню и footer. Знакомый адрес `/ueber-uns` перенаправляет на канонический `/unternehmen`. Текст о происхождении KLOTZ, клиентах, рабочем процессе и выставке профессионально переработан по [legacy-странице](https://www.klotz.mobi/ueber-uns); имена, портреты и численность команды не перенесены без подтверждения актуальности. Контент живёт в типизированном repository. Использованы уже разрешённая для презентации фотография и оригинальная SVG-схема с отключаемой анимацией; новых зависимостей нет.

Локально проверено: Prettier, TypeScript, ESLint, 21/21 unit/component tests, production build (53 записи), Playwright **32/32** на desktop/mobile. Для новой страницы проверены изображение, H1, canonical, noindex, отсутствие неподтверждённого JSON-LD, внутренние ссылки, отсутствие горизонтального переполнения и ошибок браузера, axe WCAG 2.2 AA. `/ueber-uns` возвращает 308 на `/unternehmen`. Route check: 45 HTML-страниц, 1 redirect, 45 внутренних ссылок. Визуально просмотрены новая страница на desktop/mobile и главная, услуга, референс, статья и форма на desktop.

Lighthouse mobile (главная / услуга / референс / статья / форма / «Über uns»): Performance **88 / 89 / 90 / 85 / 84 / 81**, Accessibility и Best Practices **100**, SEO **69** из-за намеренного noindex. На новой странице LCP 3,12 с, CLS 0. Цель Performance ≥95 остаётся открытой; локальные значения колеблются и не являются полевыми метриками.

Для бизнеса: **лучше** — компания представлена конкретнее, а путь к ней заметен в навигации. **Риск** — сведения о 2017 годе и выставке пока основаны на legacy-странице и требуют подтверждения KLOTZ до production. Изменения не коммитились, не отправлялись и не публиковались; production не проверялся.

### 2026-09-25 — компактный запуск AI-консультанта

По просьбе пользователя видимая надпись `Projektlotse` убрана с плавающей кнопки. Теперь это небольшой полупрозрачный круглый значок с доступным названием `KI-Beratung öffnen`; название внутри открытого диалога сохранено. На главной значок появляется спустя 5,5 секунды после открытия страницы, не открывая сам диалог. На внутренних страницах он доступен сразу, а при повторном посещении главной в рамках текущей навигации повторно не исчезает. CSS-анимация появления и два коротких световых импульса служат приглашением к действию; при reduced motion отключены. Новых зависимостей и изображений нет.

Локально проверено: форматирование, TypeScript, ESLint, 21/21 unit/component tests, production build (52 записи), Playwright 28/28 на desktop/mobile, включая задержку, доступное имя, открытие диалога, клавиатуру, axe и reduced motion. Route check: 45 HTML-страниц и 45 внутренних ссылок; Playwright также проверил canonical, metadata, noindex, robots, sitemap и JSON-LD. Визуально пересмотрены первый экран на mobile/desktop и основные типы страниц. Lighthouse mobile: Performance **79 / 85 / 81 / 85 / 90** для главной / услуги / референса / статьи / формы; Accessibility и Best Practices **100**, SEO **69** из-за намеренного noindex, CLS **0**. Порог Performance ≥95 не достигнут; эти единичные локальные замеры не доказывают влияние конкретной анимации.

Для бизнеса: **лучше** — меньше перекрыта фотография на мобильном и появление заметно без постоянного отвлечения. **Рискованнее** — значок без надписи может быть менее понятен части посетителей; это требует проверки на клиентском показе. Изменения только локальные, без commit/push/deployment и без проверки production.

### 2026-09-25 — предметная главная и FAQ

По запросу пользователя первый экран дополнили конкретные услуги и регион; эмоциональный H1 и направление редизайна v2 сохранены. Вступление, процесс и B2B-секция стали яснее. Перед финальным CTA появился FAQ из пяти вопросов: выбор террасного решения, факторы стоимости, подготовка к разговору, B2B и реальные референсы. Ответы — редакционный `draft` со ссылками на существующие разделы; без цен, сроков и выдуманных деталей объектов. Контент вынесен в типизированный repository. FAQ работает через нативный HTML без новых зависимостей и без `FAQPage` JSON-LD.

Локальная проверка: форматирование, TypeScript, ESLint, 21/21 unit/component tests, production build (52 записи), Playwright **28/28** desktop/mobile после исправления охвата axe-теста, включая клавиатуру и axe открытого FAQ. Проверены 45 HTML-страниц и 45 внутренних ссылок, H1, canonical, description, noindex, robots, sitemap и отсутствие неподтверждённого JSON-LD. Визуально пересмотрены новый первый экран и FAQ на desktop/mobile, а также главная, услуга, референс, статья и форма на mobile. Lighthouse mobile: Performance **77 / 87 / 90 / 80 / 80** для главной / услуги / референса / статьи / формы; Accessibility и Best Practices **100**, SEO **69** из-за намеренного noindex, CLS **0**. Главная ниже прошлого локального замера 84; измерения колеблются, причинность новой редактуре не установлена. Цель Performance ≥95 остаётся открытой.

Для бизнеса: **лучше** — быстрее понятно, чем занимается KLOTZ и куда идти с вопросом. **Рискованнее** — новые формулировки FAQ требуют согласования заказчика до production; текущий результат не является юридической или технической консультацией. Нет новой отправки формы, публикации, commit/push/deployment; production не проверялся.

### 2026-09-25 — читаемость диалога и язык ответа

По запросу пользователя исправлена прокрутка `Projektlotse`: после новой реплики виден её начальный текст даже при длинном ответе; фокус возвращается в поле ввода без прокрутки. Интерфейс сайта остаётся немецким. Live AI отвечает на языке последнего вопроса независимо от языка предыдущих реплик или немецкого сайта. Сценарный режим без ключа и сопровождающие подписи ссылок локализованы для DE/EN/RU. В русских/английских вопросах добавлено распознавание основных тем. Это демонстрационный словарь, не перевод всего сайта и не гарантия поддержки всех языков без AI.

Пользователь добавил ключ в локальный игнорируемый `.env`. После перезапуска production-сервера прямой синтетический вопрос «Какие варианты навеса для террасы есть в демоверсии?» получил русскоязычный ответ через `mode=live`, со ссылкой `/terrasse-garten`. Значение ключа не выводилось и не включалось в Git. Это один smoke-тест, не систематическая оценка качества AI; вызов мог потребовать платного API-ресурса.

Проверено локально: `npm run typecheck`, ESLint, Prettier, `git diff --check`, 20/20 unit/component tests, production build (52 записи), 26/26 Playwright-тестов на desktop/mobile, включая геометрию и визуальный просмотр длинного русского ответа, фокус, клавиатуру и axe открытого диалога. Визуально пересмотрены главная, услуга, референс, статья и форма на mobile, а также главная на desktop. Route check: 45 HTML-страниц и 45 внутренних ссылок, canonical, description, noindex; Playwright проверил robots, sitemap и JSON-LD. Lighthouse mobile: Performance **84 / 79 / 91 / 90 / 79** для главной / услуги / референса / статьи / формы, Accessibility и Best Practices **100**, SEO **69** из-за намеренного noindex, CLS **0**. Цель Performance ≥95 не достигнута.

Для бизнеса: **лучше** — посетитель читает длинные ответы без поиска начала и может обращаться на своём языке; **рискованнее** — live AI расходует средства, может отвечать неточно и передаёт неперсональный текст вопроса OpenAI. Требуются privacy review, мониторинг стоимости/качества и распределённая защита до публичного запуска. Commit/push/deployment и публикация не выполнялись; production не проверялся.

### 2026-09-25 — проектный консультант для демонстрации

По следующей просьбе пользователя создан локальный `.env` с пустым `OPENAI_API_KEY` и `OPENAI_MODEL="gpt-5.4-mini"`. `.gitignore` исключает `.env` из Git; сервер теперь читает переменную модели при запросе. После добавления ключа пользователем потребуется перезапуск локального сервера и реальная проверка ответа.

По новой команде пользователя добавлен `Projektlotse` на все страницы. Он помогает выбрать услугу и перейти к её странице или demo-форме. С `OPENAI_API_KEY` сервер обращается к OpenAI Responses API, prompt составлен из существующего repository, ответ показывается как обычный текст. Без ключа работает помеченный сценарный режим. Ключа в среде этого этапа нет, поэтому реальный платный вызов API локально не проверен.

Уточнение 25.09: исправлена передача контекста нескольких реплик в live-режиме. Ограниченная история из браузера передаётся модели как пользовательские данные, без доверенной роли assistant/developer; сервер проверяет формат и отбрасывает неподходящие роли. В сценарном режиме неизвестная деталь после выбранной темы получает честный контекстный ответ без догадок. После исправления повторно прошли 19 unit/component tests, typecheck, ESLint, форматирование, `git diff --check`, production build, 2/2 целевых Playwright-теста на desktop/mobile и HTTP-проверка второго вопроса. Полный набор 24/24 Playwright и Lighthouse ниже проходили непосредственно до этой локальной правки передачи истории; остальная вёрстка и маршруты не изменялись.

Ограничения: немецкий язык, запрет выдуманных цен/сроков/характеристик, предупреждение о передаче вопроса OpenAI в live-режиме, блокировка email/телефона, `store: false`, ограничения объёма и process-local лимит. Нет сохранения переписки и передачи её в форму. Перед public запуском нужны проверка privacy-текста, распределённое ограничение запросов, контроль расходов и оценка качества ответов.

Локально проверено: `npm run typecheck`, ESLint, форматирование, `git diff --check`, production build (52 записи) и 19 unit/component tests. Playwright: 24/24 на desktop и mobile, включая открытие, ответы, ссылку, перезапуск, Escape, фокус и axe для открытого диалога. Визуально проверены диалог и ответ на обоих размерах, а также пять основных типов страниц. Проверка маршрутов: 45 HTML-страниц и 45 внутренних ссылок; metadata, canonical, noindex, sitemap, robots и JSON-LD остались корректными. Прямой HTTP smoke `/api/assistant`: `guided`, осмысленный ответ, ссылка на `/terrasse-garten`. Вызов Responses API протестирован только через mock: корректный серверный ключ, модель, `store: false` и разбор ответа; с реальным ключом соединение ещё не проверено.

Lighthouse mobile после добавления виджета: главная **79**, услуга **79**, референс **73**, статья **84**, форма **80**; Accessibility/Best Practices **100**, SEO **69** из-за намеренного noindex, CLS **0**. Скорость остаётся ниже целевых 95 и колеблется между локальными прогонами.

Для бизнеса: **лучше** — наглядная ориентация по услугам и путь к разделам/заявке. **Рискованнее** — live AI при включении ключа несёт расходы, может ошибаться и передаёт вопрос внешнему провайдеру. До публичного запуска нужны контроль расходов/качества, privacy review и распределённая защита от злоупотреблений. Commit/push/deployment не выполнялись.

### 2026-09-24 — самостоятельный редакционный редизайн v2

Основание: пользователь указал, что первая версия слишком похожа на legacy, и запросил заметно новый профессиональный дизайн с уместным motion. Исходная точка этого этапа — commit `28727d2`; переинициализации проекта и замены архитектуры нет.

Сделано:

- Новый full-bleed hero с реальным изображением, крупной смешанной типографикой и координатными линиями.
- Полностью новая композиция главной: асимметричные услуги, крупные фотосерии, вертикальный процесс со sticky-вступлением, блок региона, B2B и редакционный журнал.
- Общая визуальная система внутренних страниц, формы, контактов и подвала: графит, тёплая бумага, лаймовый акцент, типографика и строгие границы.
- Progressive motion без новых пакетов: Web Animations/IntersectionObserver, однократная CSS-анимация линий, hover изображений и стрелок; поддержаны reduced motion и видимость контента без JS.
- Существующие маршруты, 21 услуга, 4 подборки, 4 статьи, original logo, manifest и безопасная форма сохранены. AI-assets и новые внешние сервисы не добавлялись.
- Новое направление записано в D-018 и visual concept; новые тексты маркируются как редакционный draft в реестре источников.

Проверки этого этапа:

- TypeScript, ESLint, форматирование и `git diff --check` — успешно; 15 unit/component tests пройдены.
- Production build — успешно, 51 сгенерированная страница/служебный маршрут в отчёте сборки.
- Playwright — 22/22 на desktop/mobile: пять типов страниц, форма и ошибки, фильтры, клавиатура, no-JS и reduced motion. После исправления двух контрастных сочетаний axe не обнаружил нарушений на проверенных страницах.
- Проверены 45 HTML-страниц и 45 внутренних ссылок, canonical, metadata, noindex, sitemap, robots и JSON-LD. Демо не индексируется намеренно.
- Визуально просмотрены главная, услуга, референс, статья и форма на desktop/mobile. Скриншоты сохранены локально в `test-results/`, не включены в Git.
- Lighthouse mobile: главная **60**, услуга **73**, референс **92**, статья **84**, форма **70**; Accessibility/Best Practices **100** на всех пяти страницах, SEO **69** из-за намеренного запрета индексации, CLS **0**. Отдельный повтор главной: Performance **66**, LCP **3,86 с**, TBT **782 мс**. Цель Performance ≥95 **не достигнута**. В отчёте главной заметны затраты Style/Layout и JavaScript; точная причинность требует профилирования. Эти результаты не заменяют проверку реальных устройств и production-метрики.

Отчёты Lighthouse находятся в `.lighthouseci/` локально. Цифры ниже относятся к предыдущим этапам и не являются итогом v2.

Бизнес-оценка: **лучше** — самостоятельный образ и выразительная подача реальных работ. **Хуже по текущим локальным замерам** — скорость относительно отчёта v1; производительность требует отдельной оптимизации и контролируемого сравнения. **Риск** — новый цветовой акцент и композиция ещё требуют одобрения заказчика; без подтверждения они не считаются brand book. Версия готова к обсуждению дизайна, не к публичному приёму заявок. Новые commit/push/deployment не выполнялись.

### 2026-09-24 — рабочий KLOTZ demo milestone

Сделано:

- Создан Next.js 16 App Router проект с типизированным локальным content repository.
- Реализована responsive premium-главная, мобильная навигация и sticky contact bar.
- Созданы маршруты услуг, референсов, Wissen, компании, контактов и юридических заглушек.
- Добавлены metadata, canonical, Open Graph, LocalBusiness JSON-LD, sitemap, robots и manifest.
- Реализована безопасная интерактивная форма в demo-режиме без передачи персональных данных.

Проверено:

- `npm run typecheck` — успешно.
- `npm run lint` — успешно.
- `npm test` — 3 теста успешно.
- `npm run build` — 27 страниц сгенерированы успешно.
- Основные публичные маршруты — HTTP 200.
- Browser QA: изображения загружаются, форма работает, console errors отсутствуют.

Не сделано / риски:

- Не получены права на legacy-фотографии и исходный векторный логотип.
- Не выполнен полный legacy crawl и redirect map.
- Проектные факты, legal copy и контакты требуют клиентского подтверждения.
- Нет production-интеграции формы, аналитики, CRM, Neon или Prisma repository.

Следующий безопасный шаг:

- Совместно просмотреть демо, зафиксировать правки по дизайну и получить исходные материалы KLOTZ.

## Формат обновления после каждой рабочей сессии

### 2026-09-24 — содержательная клиентская презентация

Эта запись уточняет предыдущий milestone: отсутствие demo-прав на материалы больше не является блокером, LocalBusiness JSON-LD удалён, индексация принудительно запрещена.

Сделано:

- Главная переработана в светлом архитектурном стиле: оригинальный логотип, отдельная реальная hero-фотография, 4 направления, проекты, процесс, регион, B2B, Wissen и контактный блок.
- 21 страница услуг за типизированным repository, 4 реальные фотоподборки с галереями, фильтр референсов, 4 полные немецкие статьи и 12 тем редакционного плана.
- 19 legacy-assets локально оптимизированы в WebP с manifest источников/прав/SHA-256. Изображения для hero и галерей успешно извлечены; генерация не понадобилась.
- Форма: общая клиентская/серверная валидация, ошибки и управление фокусом, сохранение заполненных полей при сбое. Metadata файлов проверяется, содержимое файлов не отправляется. Нет хранения/доставки заявок.
- Диалог мобильного меню с фокусом/Escape, адаптивные карточки, контактная панель не перекрывает форму/подвал.
- Исправлены найденные QA дефекты: переполнение на 320 px, дублирующиеся breadcrumbs, ошибочный 403 при нормализации локального hostname в Next. Проверка Origin сравнивается с HTTP Host, чужие Origin запрещены; reverse proxy отдельно не настроен.
- Добавлены воспроизводимые e2e/axe/Lighthouse и route checks. Отчёты исключены из Git, ESLint и TypeScript не сканируют сгенерированные QA-артефакты.
- Подготовлен `docs/CLIENT_PRESENTATION.md`, обновлены решения, источники и открытые вопросы.

Проверено локально на Node 24.21.0:

- TypeScript, ESLint — успешно; 15 unit/component tests — успешно.
- Production build — успешно, 51 сгенерированная запись (включая технические маршруты).
- `node scripts/check-routes.mjs` — 45 HTML-страниц и 45 уникальных внутренних ссылок HTTP 200; H1, canonical, description, noindex проверены.
- `npm audit` — 0 обнаруженных уязвимостей.
- `npm run format:check`, `npm run lint`, `git diff --check` — успешно.
- Финальный Playwright — **18/18**, desktop Chromium 1440×1000 и mobile 390×844; дополнительно ширины 320/768/1440/1920. Форма, отказ сервера/повторная попытка, сохранение значений, фокус ошибок/успеха, Tab в меню/Escape, фильтр, 404 и robots/sitemap — успешно.
- Axe WCAG 2 A/AA, 2.1 AA, 2.2 AA — 0 обнаруженных нарушений на главной, услуге, референсе, статье и форме в обеих конфигурациях. Изображения загружены, горизонтальное переполнение и ошибки JavaScript/console на этих страницах не обнаружены.
- Выполнена визуальная проверка тех же 5 типов страниц: desktop full-page и mobile viewport. Скриншоты — `test-results/`, дополнительные previews — `test-results/previews/`.
- Lighthouse 13.5, mobile simulated throttling, локальный production build: результаты ниже. Цель Performance ≥95 **не достигнута**, production sign-off не даётся.

| Страница                            | Performance | Accessibility | Best Practices | SEO | LCP, s | CLS |
| ----------------------------------- | ----------: | ------------: | -------------: | --: | -----: | --: |
| Главная                             |          78 |           100 |            100 |  69 |   3.69 |   0 |
| Terrassenüberdachungen              |          74 |           100 |            100 |  69 |   3.16 |   0 |
| Glasschiebewände (референс)         |          76 |           100 |            100 |  69 |   3.30 |   0 |
| Lamellendach oder Glasdach (статья) |          94 |           100 |            100 |  69 |   1.44 |   0 |
| Форма                               |          88 |           100 |            100 |  69 |   2.75 |   0 |

Это последний полный замер, не выбранные лучшие значения. SEO 69 обусловлен намеренным `noindex`; индексирование не включалось ради оценки. Performance меняется между локальными прогонами; дальнейший приоритет — время выполнения framework JavaScript, отрисовка и подбор размеров изображений. Полевых p75/INP нет: сайт не публиковался. Отчёты `.lighthouseci/*.json`, сводка `.lighthouseci/summary.json`.

После первого замера устранены причины browser console/CSP ошибок: добавлена иконка, Zod переведён в jitless-режим. Лишние ~87 KB сжатого Zod на главной больше не загружаются автоматически. Последний Lighthouse фиксирует Best Practices 100 на всех 5 страницах. При cleanup обнаружено отсутствие System32 в PATH среды: audit-браузер был остановлен адресно, только его дерево процессов; для скрипта добавлен корректный путь к taskkill. Отдельный повторный тест запуска/остановки audit-браузера прошёл. Широкое удаление временных папок не выполнялось, сохранённые отчёты не затронуты.

Ограничения перед production:

- Полный legacy-crawl и redirect map ещё не созданы. Права на публичный перенос, актуальность контактов, legal copy, авторство и предметная проверка статей требуют заказчика.
- Нет реальной отправки формы, хранения файлов, CMS/CRM/БД/аналитики. Ограничение запросов process-local; это не production-infrastructure.
- Ручной screen-reader, Safari/Firefox и физические устройства не проверялись. Автоматический axe не является сертификатом WCAG.
- Performance ≥95 остаётся открытой целью; локальная клиентская презентация не равна полной production-приёмке.
- Не выполнялись новые push/deployment/публикация/DNS. Noindex не является защитой доступа: будущему внешнему preview нужен пароль.

Для бизнеса: **лучше** — узнаваемость, реальные материалы, содержательный путь к заявке и проверенный demo-flow. **Рискованнее** было бы запускать публично до правовой/редакционной проверки; такой запуск не выполнялся.

Следующий безопасный шаг: показать локальную версию клиенту по сценарию, согласовать дизайн, приоритеты услуг и факты референсов.

## Шаблон записи

Добавлять запись:

```text
### YYYY-MM-DD — краткое название этапа

Сделано:
- ...

Проверено:
- команда/страница/результат

Не сделано / риски:
- ...

Следующий безопасный шаг:
- ...
```
